import { Inter, Lusitana, Arima } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

export const lusitana = Lusitana({
    weight: ['400', '700'],
    subsets: ['latin'],
});

export const arima = Arima({
    weight: ['400', '700'],
    subsets: ['latin'],
});
